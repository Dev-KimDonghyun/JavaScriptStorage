let hello = '안녕하세요';

hello.length();
// 길이 구하기

hello[2];
// 특정 글자에 접근

let hi = 'Hello, Nice to meet you. My name is Donghyun Kim';

hi.toUpperCase(); // 전부 대문자로
hi.toLowerCase(); // 전부 소문자로

hi.indexOf('name'); // name이 몇번째에 위치하는지 알려줌.

hi.slice(a, b); // a에서 b까지 추출

hi.substr(c, d); // c부터 d개 추출

hi.trim(); // 공백 비우기

hi.repeat(5); // 문자열 5번 반복